# Agent Passport

Declare only public identity and capability metadata. Validate issuer, version,
expiry, status, supported protocol revisions, keys, and revocation. Never place
credentials, bearer tokens, private endpoints, customer data, or hidden policy
in a Passport.

