# Data Contract design

Allow only required fields and classes. Explicitly prohibit secret-like and
irrelevant fields. Bound bytes, string length, array length, and object depth.
Project and redact at every agent boundary.

