# Testing

Use deterministic synthetic fixtures and loopback targets. Test malformed
messages, unsupported versions, expiry, tenant isolation, workspace isolation,
replay, idempotency conflict, cancellation, deadline, delegation exhaustion,
approval expiry, Receipt tampering, and revocation. Do not call a live provider.

