# Protocol overview

Ghost Bridge Native follows discovery, version negotiation, Passport
verification, capability discovery, one-time installation, scoped invocation,
optional delegation and approval, durable Tasks, Receipts, and revocation.

Protocol and SDK versions are independent. `ghostbridge/0.1-draft` is
experimental.

