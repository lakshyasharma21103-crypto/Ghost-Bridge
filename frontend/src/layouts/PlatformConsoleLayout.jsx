import { Layout } from '../components/Layout.jsx';

/**
 * Authenticated commercial Platform boundary. The historical Layout component
 * remains the implementation so saved Console links keep their behavior.
 */
export function PlatformConsoleLayout() {
  return <Layout />;
}
