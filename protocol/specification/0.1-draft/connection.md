# Agent Connection

A Connection Offer names the Agent and Passport, negotiated protocol,
transport category, non-secret runtime reference, authentication mode and setup
reference, expiry, accepted tenant scope, restrictions, revocation reference,
and optional proof.

Authentication secrets are provisioned out of band or through a compatible
credential broker and are never part of the offer. Ordinary installation does
not require manually entering runtime endpoint URLs.
