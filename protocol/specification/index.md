# Ghost Bridge specifications

> This index is non-normative. It is a navigation aid only and never defines,
> amends, or selects protocol semantics.

| Specification | Status | Location |
| --- | --- | --- |
| `ghostbridge/0.1-draft` | Legacy historical draft; preserved with its original meaning | [`0.1-draft/`](./0.1-draft/) |
| `ghostbridge/e1.r0-draft.1` | Current normative-writing candidate; draft/prerelease, incomplete, and not Protocol 1.0 | [`e1.r0-draft.1/`](./e1.r0-draft.1/) |

Versioned specification history is immutable. A later draft or release does not
rename, normalize, backfill, or reinterpret an earlier directory or an object
created under an earlier protocol identity.

The protocol release selected under the versioned specification defines protocol
semantics. Package versions, SDK versions, implementation builds, Platform or
other deployment releases, Git branches, and this mutable index do not define or
change those semantics.

An immutable release manifest and a separate append-only errata reference may be
linked here after those artifacts are created and authorized. No manifest ID,
errata ID, artifact digest, or publication status is assigned by this placeholder.
