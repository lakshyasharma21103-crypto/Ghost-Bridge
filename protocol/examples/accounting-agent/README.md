# Accounting Agent

Deterministic Ghost Bridge Native reference agent implementing
`accounting.check_duplicate` and `accounting.create_draft`. Both capabilities
require bounded Delegation Grants; draft creation also requires an exact,
single-use Approval Decision.
