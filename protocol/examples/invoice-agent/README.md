# Invoice Extraction Agent

Deterministic Ghost Bridge Native reference agent implementing `invoice.extract`.
It uses synthetic data only and calls no model, paid provider, production database,
or external service.
